// import {createCookie} from '@shopify/react';

// export const localizationCookie = createCookie('localization', {
//   path: '/',
//   httpOnly: true,
//   secure: true,
// });

// export const themeCookie = browser.cookie('theme', {
//   path: '/',
//   httpOnly: true,
//   secure: true,
// });

// export const getThemeCookie = await browser.cookie.get('theme');
// export const setThemeCookie = await browser.cookie.set('theme', 'ABCX123');
